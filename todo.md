- display creation error messages in tab create dialog
- pasting does not work (neither ctrl-v not alt-v)
- when creating branch ensure that neither working tree nor branch with that name exists
- skippermissions frage führt zu schliessen des tabs bei erstmalligem verweden
- npm -g install hat bei stefan nicht funktioniert
- ost wird weder das tatsächliche abreitsverzeichnis noch der branch gelöscht
  (bei git worktree list erscheint der branch allerdings nicht mehr)
- Bei click auf "x" bei Unmerged soll im Dialog auch der "Merge" angeboten werden.